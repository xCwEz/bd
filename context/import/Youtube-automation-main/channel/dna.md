# Channel DNA (human-readable)

This mirrors `data/channel.json`. Edit either; keep them roughly in sync after your first real style lock.

## Niche

**History's almosts** — near-misses that almost changed everything.

## Format

- 9:16 Shorts
- Animated (faceless narrator)
- ~45–60 seconds
- Subtitles on (burned in at mux)
- Pipeline: script → ElevenLabs VO → 5× Kling image (9:16) → 5× Kling 2.1 I2V → ffmpeg mux

## Style

Cinematic storybook / documentary-adjacent. Non-photoreal. Lock one house style on your first real render, then reuse it forever.

## Voice

One external narrator via ElevenLabs API (`eleven_multilingual_v2`). Characters never talk on screen.
Default voice: Rachel (`21m00Tcm4TlvDq8ikWAM`) — change on Setup.

## Cadence

3 Shorts / week (suggested).

## Style lock (fill after first real render)

- Style preset / key:
- Voice id / pair:
- Notes:
